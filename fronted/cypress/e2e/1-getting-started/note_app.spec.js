describe('note App',()=>{
    it('frontpage can be opened', () => {
        cy.visit('http://localhost:8000/')
        cy.contains('Calendario')
    })
})

describe('note App',()=>{
    it('frontpage can be opened', () => {
        cy.visit('http://localhost:8000/')
        cy.contains('Calendario')
    })
})